name: optuna

entry_points:
  main:
    parameters:
      command:
        description: Enter command to execute
        type: str
        default: jupyterlab

    command: python -m {command} --allow-root
